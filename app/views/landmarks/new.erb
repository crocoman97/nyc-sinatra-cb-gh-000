
<h1>Create a New Landmark</h1>
<form method="post" action="/landmarks">
  
  <p>Name: <input type="text" name="landmark[name]" id="figure_name"></p>
  <p>Year_Completed: <input type="text" name="landmark[name]" id="figure_name"></p>
  <% Landmark.all.each do |landmark| %>
    <input type="checkbox" name="landmark[landmark_ids][]" id="landmark_<%= landmark.id %>" value="<%= landmark.id %>"><%= landmark.name %></input>
  <% end %>
  
  <br></br>
  
  <p>Or Create a new landmark</p>
  <p>landmark: <input type="text" name="landmark[name]" id="new_landmark"></p>
  <br></br>
  
  <% Title.all.each do |title| %>
    <input type="checkbox" name="landmark[title_ids][]" id="title_<%= title.id %>" value="<%= title.id %>"><%= title.name %></input>
  <% end %>
  
  <br></br>
  
  <p>or create a new title</p>
  
  <p>title: <input type="text" name="title[name]" id="new_title"></p>
  
  <input type="submit" value="Create New Figure">
  
</form>